# v0.1.3 verification

Source/tag: `db000a6` / `v0.1.3`. Published on 2026-09-26 (JST).

- Reproduced the defect: a second fetch at 13:00 removed two existing morning/ongoing appointments. After the fix, all three source appointments remain and deletion count is zero.
- COM restriction, Rust filtering, and SQLite reconciliation use the same inclusive date range. Tests cover midnight, the full last day, and year rollover.
- Rust: 71 passed, one live Outlook integration test ignored. Clippy passed. Outlook was not running, so no live Outlook synchronization or user-data repair was performed.
- Native WebView: 57 checks passed with the debug build and again with the app extracted from the signed NSIS installer, using isolated data.
- Installer signature verified with the committed public key; altered installer content was rejected.
- Independent review found no material issues in the bounded fix.
- GitHub CI `36152497146` passed. The redundant Windows Release run `36152500243` was cancelled before publishing locally built, verified artifacts.
- Anonymous installer download matched the tested file. A real v0.1.2 native updater detected the public v0.1.3 feed. Installation was left to the user.

Release: https://github.com/t-ueda21/TaskCalendar-Plus/releases/tag/v0.1.3
