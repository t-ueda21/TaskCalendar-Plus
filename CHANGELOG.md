# Changelog

## [0.1.2] - 2026-09-25

- 更新ボタンをタブの右側へ配置。日付・現在時刻・定時までの表示を同じ段に維持。
- 起動時に更新が見つかった場合、更新内容のお知らせを表示。
- 設定から更新する際は、設定を保存して閉じてから更新画面へ移動。保存に失敗した場合は更新を開始しない。
- 更新状態・画面表示・ネイティブ連携の処理を整理し、重複したスタイルを統合。

## [0.1.1] - 2026-09-25

- アプリアイコンを新しい画像へ変更。
- 設定の「説明」にGitHubへのリンクを追加。
- アップデート欄の余白を減らし、バージョンと確認ボタンを横並びに変更。

## [0.1.0] - 2026-09-25

最初の公開版。特定の組織向けに作っていたアプリをもとに、その組織に依存する設定・文言・機能を取り除いて一般化した。

### 追加

- AIモードを、AI(Claude Code / Codex)がアプリの記録を自分で調べて答えるエージェント方式にした。アプリはMCPサーバーを内蔵し、予定の検索・作業時間の集計・月の状況(タグ予算の残り・あと使える時間)・日次サマリーの取得などの道具をAIに渡す。時間の集計はアプリが計算する。
- AIモードでの予定の作成・変更・削除は、AIが提案し、チャットの確認カードで［確定］したときだけ反映するようにした。
- Claude Code / Codex の「検出」ボタン(バージョンの確認、動作確認済みより古い場合の警告)。
- ヘッダーに表示するクイックリンク。
- 設定とタグのエクスポート/インポート(JSON)。
- 天気の地点を都道府県庁所在地47か所から選べるようにした(既定は東京)。
- 全データ(タスク・タグ・設定・日次サマリー・気づきメモ)のバックアップ/復元。
- 設定ファイル・バックアップの取り込み時に、開くURL・クイックリンク・AIの接続先を確認画面に表示する。
- AIの応答待ちの間、経過秒数を表示する。
- 実際のアプリを起動して確かめるスモークテスト(`scripts/e2e-smoke.mjs`)と、GitHub ActionsのCI。
- Claude Code / Codex との連携スイッチ(既定はオフ、説明付き)。オフの間は接続先に選べず、アプリ側でも呼び出さない。
- Claude Code / Codex のモデルと effort を設定画面で選べるようにした(effort の既定は low)。AIモード画面に effort と実際に使われたモデル名を表示する。
- AIの回答をMarkdown(太字・箇条書き・表・コードなど)で表示するようにした(HTMLはエスケープしてから記法だけを変換する)。

### 変更

- 指定時刻に特定の社内ページを開く機能を、任意のURLを開く「URL自動オープン」にした(既定はOFF)。
- 会社独自の休日の名称を「会社休日」にした。
- 既定のタグをなくした。稼働時間の既定値を 9:00–18:00(昼休み 12:00–13:00 は工数から除外)にした。
- Outlook自動同期の既定をOFFにした。
- アプリ名・アイコン・識別子を変更した。
- 内蔵HTTP APIを、起動ごとの合言葉と `Host` / `Origin` の確認で保護した(ブラウザで開いた他のWebページから使われないようにするため)。

### 修正


- タグが最後の1件のときに削除できなかった。
- 存在しないAIモデル名へフォールバックしていた。
- タグを同じ瞬間に複数作るとIDが重複することがあった。
- タグを削除しても、月ごとのタグ順とOutlookの取り込み先タグの設定に削除済みのIDが残っていた。

### 削除

- 正規表現で意図を判定して決まった手順に流し込む、旧来のAIモードの対話処理。
- Ollama(ローカルLLM)への接続。AIは Claude Code / Codex だけにした。
- AIを使わない場合の定型の回答・簡易版の日次サマリー。AIを設定しない場合、AIモードと日次サマリーは使えない。
- 使われていなかった旧画面(1画面1ページ構成)と、使われていなかった埋め込みAPI(`/api/ai/embed`)。
- 使われていなかったAPI(`/api/app-icon`、日次サマリー・気づきメモの1日分の取得)と、画面・スタイルの未使用コード。
